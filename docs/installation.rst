============
Installation
============

At the command line::

    $ easy_install generalwords

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv generalwords
    $ pip install generalwords
